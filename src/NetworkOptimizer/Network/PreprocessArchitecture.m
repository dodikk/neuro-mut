function [ result ] = PreprocessArchitecture( architecture )
% Actions, necessary for the correct work of the propagation algorithm
%

    result = architecture;
    %result.inputs = sort(result.inputs);
    %result.outputs = sort(result.outputs);

return
end
