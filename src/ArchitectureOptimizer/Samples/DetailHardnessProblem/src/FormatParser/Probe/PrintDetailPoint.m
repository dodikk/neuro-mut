function[] = PrintDetailPoint(point)

    msgRadius   = sprintf('Radius   : %f', point.radius  );
    msgHardness = sprintf('Hardness : %f', point.hardness);
    
    disp(msgRadius);
    disp(msgHardness);

return
end
