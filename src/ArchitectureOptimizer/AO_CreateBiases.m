function [biases] = CreateBiases(net)

    biases.low = [];
    biases.up = [];
    biases.indexes = [];

%     vertexCount = size(net.connections, 2);
% 
%     bIndex = 1 : 1 : vertexCount;
%     bIndex = setdiff(bIndex, net.inputs);
%     biasCount = size(bIndex, 2);
%     
%     biases.indexes = bIndex;
%     biases.low = ones(1, biasCount) * (-10); 
%     biases.up = ones(1, biasCount) * (+10); 

return
end
